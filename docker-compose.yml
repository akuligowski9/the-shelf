services:
  db:
    image: postgres:16
    container_name: shelf_db
    environment:
      POSTGRES_USER: shelf
      POSTGRES_PASSWORD: shelf
      POSTGRES_DB: shelf
    ports:
      - "5432:5432"
    volumes:
      - shelf_pgdata:/var/lib/postgresql/data

volumes:
  shelf_pgdata:
